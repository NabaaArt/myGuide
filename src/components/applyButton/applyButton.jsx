"use client";
import { useState } from "react";
import "./applyButton.module.css";

const { jobs, setJobs } = useState();

let qt = jobs.find((el) => el.job.id === item.id);


const applyButton = ({ item }) => {

}

export default applyButton;